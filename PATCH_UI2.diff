diff -ruN apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/admin.css apollus_finance_nfse_v2_ui_clean/admin/admin.css
--- apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/admin.css	2026-08-07 18:29:22.000000000 +0000
+++ apollus_finance_nfse_v2_ui_clean/admin/admin.css	2026-08-07 19:58:56.760012376 +0000
@@ -2618,68 +2618,285 @@
 
 
 
-/* ---------- NFS-e V2 ---------- */
+
+/* ---------- NFS-e V2 — interface limpa ---------- */
+.invoice-compact-section {
+  display: grid;
+  gap: 12px;
+  padding: 14px 15px;
+}
+
 .invoice-master-toggle {
-  padding: 2px 0 8px;
+  padding: 0;
 }
 
-.invoice-v2 {
+.invoice-summary-card {
   display: grid;
-  gap: 14px;
-  margin-top: 12px;
+  grid-template-columns: minmax(0, 1fr) auto;
+  gap: 18px;
+  align-items: center;
+  padding: 16px;
+  border: 1px solid rgba(16,16,16,.12);
+  border-radius: 16px;
+  background: rgba(255,255,255,.72);
 }
 
-.invoice-v2-heading {
-  padding: 15px 16px;
-  border: 1px solid rgba(16,16,16,.12);
-  border-radius: 15px;
-  background: linear-gradient(135deg, rgba(189,135,255,.13), rgba(119,228,223,.08));
+.invoice-summary-copy {
+  min-width: 0;
 }
 
-.invoice-v2-heading h3,
-.invoice-block h4,
-.invoice-party-editor h4 {
+.invoice-summary-heading {
+  display: flex;
+  gap: 7px;
+  align-items: center;
+  flex-wrap: wrap;
+  margin-bottom: 7px;
+}
+
+.invoice-summary-badge,
+.invoice-summary-status {
+  display: inline-flex;
+  align-items: center;
+  min-height: 24px;
+  border-radius: 999px;
+  padding: 4px 9px;
+  font-size: .66rem;
+  font-weight: 900;
+  letter-spacing: .04em;
+}
+
+.invoice-summary-badge {
+  background: rgba(16,16,16,.08);
+}
+
+.invoice-summary-badge.emitida { background: rgba(189,135,255,.18); color: #614084; }
+.invoice-summary-badge.recebida { background: rgba(119,228,223,.20); color: #245f5d; }
+.invoice-summary-badge.intermediada { background: rgba(255,225,45,.25); color: #675800; }
+
+.invoice-summary-status.pendente { background: rgba(242,170,37,.16); color: #80530a; }
+.invoice-summary-status.emitida { background: rgba(49,185,107,.15); color: #216a42; }
+.invoice-summary-status.cancelada { background: rgba(16,16,16,.08); color: #666; }
+.invoice-summary-status.substituida { background: rgba(189,135,255,.16); color: #614084; }
+
+.invoice-summary-copy > strong {
+  display: block;
+  font-size: .96rem;
+}
+
+.invoice-summary-copy > p {
+  margin: 6px 0 4px;
+  color: var(--muted);
+  font-size: .78rem;
+  line-height: 1.45;
+}
+
+.invoice-summary-copy > small {
+  color: var(--muted);
+  font-size: .69rem;
+}
+
+.invoice-open-button {
+  min-width: 190px;
+  white-space: nowrap;
+}
+
+.invoice-dialog {
+  width: min(980px, calc(100% - 24px));
+}
+
+.invoice-dialog-card {
+  width: 100%;
+  gap: 0;
+  padding: 0;
+  overflow: hidden;
+  background: var(--paper);
+}
+
+.invoice-dialog-header {
   margin: 0;
+  padding: 24px 26px 18px;
+  border-bottom: 1px solid var(--line);
+  background: rgba(255,255,255,.55);
 }
 
-.invoice-v2-heading p:last-child {
-  margin: 7px 0 0;
+.invoice-dialog-header h2 {
+  font-size: clamp(2.5rem, 5vw, 4.2rem);
+}
+
+.invoice-dialog-header p:last-child {
+  max-width: 690px;
+  margin: 8px 0 0;
   color: var(--muted);
   font-size: .78rem;
-  line-height: 1.55;
+  line-height: 1.5;
 }
 
-.invoice-role-note {
-  margin: -4px 0 0;
+.invoice-v2 {
+  min-height: 0;
+  display: grid;
+  grid-template-rows: auto minmax(0, 1fr) auto;
+}
+
+.invoice-tabs {
+  display: grid;
+  grid-template-columns: repeat(4, 1fr);
+  gap: 6px;
   padding: 10px 12px;
+  border-bottom: 1px solid var(--line);
+  background: #fffaf3;
+}
+
+.invoice-tab {
+  min-height: 42px;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  gap: 8px;
+  border: 0;
   border-radius: 12px;
-  background: rgba(189,135,255,.10);
-  color: #5f437b;
-  font-size: .75rem;
-  font-weight: 700;
-  line-height: 1.45;
+  padding: 8px 10px;
+  background: transparent;
+  color: var(--muted);
+  font-size: .74rem;
+  font-weight: 800;
+}
+
+.invoice-tab span {
+  width: 22px;
+  height: 22px;
+  display: inline-grid;
+  place-items: center;
+  border-radius: 50%;
+  background: rgba(16,16,16,.07);
+  font-size: .65rem;
+}
+
+.invoice-tab.active {
+  background: var(--ink);
+  color: white;
+}
+
+.invoice-tab.active span {
+  background: var(--purple);
+  color: var(--ink);
+}
+
+.invoice-step {
+  min-height: 0;
+  overflow-y: auto;
+  padding: 24px 26px 28px;
+}
+
+.invoice-step[hidden] {
+  display: none !important;
+}
+
+.invoice-step-intro {
+  margin-bottom: 20px;
+}
+
+.invoice-step-intro-row {
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: 16px;
+}
+
+.invoice-step-intro h3 {
+  margin: 0;
+  font-size: 1.25rem;
+}
+
+.invoice-step-intro p:last-child {
+  max-width: 700px;
+  margin: 7px 0 0;
+  color: var(--muted);
+  font-size: .78rem;
+  line-height: 1.5;
 }
 
-.invoice-block {
+.invoice-direction-cards {
   display: grid;
-  gap: 12px;
-  padding: 15px;
+  grid-template-columns: repeat(3, 1fr);
+  gap: 10px;
+  margin-bottom: 18px;
+}
+
+.invoice-choice {
+  position: relative;
+  display: block !important;
+  min-width: 0;
+}
+
+.invoice-choice input {
+  position: absolute;
+  opacity: 0;
+  pointer-events: none;
+}
+
+.invoice-choice > span {
+  min-height: 88px;
+  display: grid;
+  align-content: center;
+  gap: 5px;
   border: 1px solid var(--line);
   border-radius: 15px;
-  background: rgba(255,255,255,.66);
+  padding: 14px;
+  background: rgba(255,255,255,.72);
+  transition: .18s ease;
 }
 
-.invoice-block-header {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  gap: 12px;
+.invoice-choice > span strong {
+  font-size: .82rem;
 }
 
-.invoice-block-header .admin-kicker {
-  margin-bottom: 4px;
+.invoice-choice > span small {
   color: var(--muted);
-  font-size: .62rem;
+  font-size: .7rem;
+  line-height: 1.35;
+}
+
+.invoice-choice input:checked + span {
+  border-color: var(--ink);
+  background: white;
+  box-shadow: 4px 4px 0 var(--purple);
+  transform: translate(-2px, -2px);
+}
+
+.invoice-identification-grid {
+  margin-top: 2px;
+}
+
+.invoice-advanced {
+  margin-top: 18px;
+  border-top: 1px solid var(--line);
+  padding-top: 13px;
+}
+
+.invoice-advanced summary {
+  cursor: pointer;
+  color: #6b4a8f;
+  font-size: .75rem;
+  font-weight: 800;
+}
+
+.invoice-advanced > div {
+  display: grid;
+  grid-template-columns: minmax(220px, 320px) minmax(0, 1fr);
+  gap: 12px;
+  align-items: end;
+  margin-top: 12px;
+}
+
+.invoice-role-note {
+  margin: 0;
+  padding: 11px 13px;
+  border-radius: 12px;
+  background: rgba(189,135,255,.10);
+  color: #5f437b;
+  font-size: .72rem;
+  font-weight: 700;
+  line-height: 1.45;
 }
 
 .invoice-parties-grid {
@@ -2688,14 +2905,28 @@
   gap: 10px;
 }
 
-.invoice-party-field {
-  display: grid;
-  gap: 7px;
+.invoice-party-card {
+  position: relative;
   min-width: 0;
+  display: grid;
+  gap: 10px;
+  padding: 15px;
+  border: 1px solid var(--line);
+  border-radius: 16px;
+  background: rgba(255,255,255,.72);
 }
 
-.invoice-party-field > label {
-  min-width: 0;
+.invoice-party-number {
+  position: absolute;
+  top: 10px;
+  right: 12px;
+  color: rgba(16,16,16,.22);
+  font-family: "Bebas Neue", sans-serif;
+  font-size: 1.8rem;
+}
+
+.invoice-party-card label {
+  padding-right: 20px;
 }
 
 .invoice-party-edit {
@@ -2710,20 +2941,15 @@
   text-underline-offset: 3px;
 }
 
-.invoice-help {
-  color: var(--muted);
-  font-size: .72rem;
-  line-height: 1.45;
-}
-
 .invoice-party-editor {
   display: grid;
-  gap: 12px;
-  margin-top: 4px;
-  padding: 14px;
-  border: 2px dashed rgba(16,16,16,.2);
-  border-radius: 14px;
+  gap: 14px;
+  margin-top: 16px;
+  padding: 16px;
+  border: 1px solid var(--line);
+  border-radius: 16px;
   background: #fffaf3;
+  box-shadow: inset 4px 0 0 var(--cyan);
 }
 
 .invoice-party-editor > header {
@@ -2733,6 +2959,10 @@
   gap: 12px;
 }
 
+.invoice-party-editor h4 {
+  margin: 0;
+}
+
 .invoice-party-editor > header button {
   width: 34px;
   height: 34px;
@@ -2751,6 +2981,27 @@
   flex-wrap: wrap;
 }
 
+.invoice-values-highlight {
+  display: grid;
+  grid-template-columns: repeat(4, minmax(0, 1fr));
+  gap: 10px;
+  margin-bottom: 18px;
+}
+
+.invoice-values-highlight label {
+  padding: 14px;
+  border: 1px solid var(--line);
+  border-radius: 15px;
+  background: rgba(255,255,255,.72);
+}
+
+.invoice-document-card {
+  display: grid;
+  grid-template-columns: minmax(0, 1.3fr) minmax(0, 1fr);
+  gap: 14px;
+  padding-top: 4px;
+}
+
 .invoice-drive-field > div {
   display: grid;
   grid-template-columns: minmax(0, 1fr) auto;
@@ -2774,6 +3025,22 @@
   white-space: nowrap;
 }
 
+.invoice-dialog-actions {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 14px;
+  padding: 14px 18px;
+  border-top: 1px solid var(--line);
+  background: rgba(255,255,255,.72);
+}
+
+.invoice-dialog-actions > span {
+  color: var(--muted);
+  font-size: .7rem;
+  line-height: 1.35;
+}
+
 .invoice-status.nao_necessaria {
   background: rgba(255,255,255,.06);
   color: rgba(255,255,255,.45);
@@ -2785,11 +3052,60 @@
 .invoice-status.substituida { background: rgba(189,135,255,.15); color: #cfadff; }
 
 @media (max-width: 900px) {
+  .invoice-summary-card { grid-template-columns: 1fr; }
+  .invoice-open-button { width: 100%; }
   .invoice-parties-grid { grid-template-columns: 1fr; }
+  .invoice-values-highlight { grid-template-columns: repeat(2, 1fr); }
+  .invoice-document-card { grid-template-columns: 1fr; }
 }
 
-@media (max-width: 640px) {
-  .invoice-block-header { align-items: flex-start; flex-direction: column; }
-  .invoice-drive-field > div { grid-template-columns: 1fr; }
-  #finance-invoice-open-drive { width: 100%; }
+@media (max-width: 720px) {
+  .invoice-tabs {
+    grid-template-columns: repeat(2, 1fr);
+  }
+  .invoice-direction-cards {
+    grid-template-columns: 1fr;
+  }
+  .invoice-step-intro-row {
+    flex-direction: column;
+  }
+  .invoice-step-intro-row .mini-action {
+    width: 100%;
+  }
+  .invoice-advanced > div {
+    grid-template-columns: 1fr;
+  }
+  .invoice-dialog-actions {
+    align-items: stretch;
+    flex-direction: column;
+  }
+  .invoice-dialog-actions .admin-btn {
+    width: 100%;
+  }
+}
+
+@media (max-width: 520px) {
+  .invoice-dialog-header {
+    padding: 20px 18px 14px;
+  }
+  .invoice-step {
+    padding: 20px 18px 24px;
+  }
+  .invoice-tabs {
+    grid-template-columns: 1fr 1fr;
+    padding: 8px;
+  }
+  .invoice-tab {
+    justify-content: flex-start;
+    font-size: .68rem;
+  }
+  .invoice-values-highlight {
+    grid-template-columns: 1fr;
+  }
+  .invoice-drive-field > div {
+    grid-template-columns: 1fr;
+  }
+  #finance-invoice-open-drive {
+    width: 100%;
+  }
 }
diff -ruN apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/dashboard.html apollus_finance_nfse_v2_ui_clean/admin/dashboard.html
--- apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/dashboard.html	2026-08-07 18:29:54.000000000 +0000
+++ apollus_finance_nfse_v2_ui_clean/admin/dashboard.html	2026-08-07 19:58:56.759281371 +0000
@@ -10,9 +10,9 @@
 <link href="https://fonts.googleapis.com" rel="preconnect"/>
 <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
 <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&amp;family=Inter:wght@400;500;600;700;800&amp;family=Permanent+Marker&amp;display=swap" rel="stylesheet"/>
-<link href="admin.css?v=20260807-stable1-nfse-v2" rel="stylesheet"/>
+<link href="admin.css?v=20260807-stable1-nfse-v2-ui2" rel="stylesheet"/>
 </head>
-<body class="admin-dashboard-body" data-admin-page="dashboard" data-apollus-version="20260807-stable1-nfse-v2">
+<body class="admin-dashboard-body" data-admin-page="dashboard" data-apollus-version="20260807-stable1-nfse-v2-ui2">
 <div class="admin-boot" id="admin-loading">
 <span class="admin-spinner"></span>
 <p>Carregando painel...</p>
@@ -688,85 +688,197 @@
 <label><span>Intervalo entre parcelas</span><select id="finance-entry-interval"><option value="0">Mesmo vencimento</option><option selected="" value="1">Mensal</option><option value="2">A cada 2 meses</option><option value="3">Trimestral</option></select></label>
 </div>
 <label><span>Observações</span><textarea id="finance-entry-notes" rows="3"></textarea></label>
-<section class="invoice-form-section" data-requires-permission="finance.invoice">
-<label class="settings-toggle invoice-master-toggle">
-<input id="finance-invoice-required" type="checkbox"/>
-<span><strong>Controlar Nota Fiscal / NFS-e</strong><small>Registra os dados fiscais e somente o link da nota no Drive. Nenhum PDF ou XML é enviado ao Supabase.</small></span>
-</label>
-<div class="invoice-v2" hidden="" id="finance-invoice-fields">
-  <div class="invoice-v2-heading">
-    <div><p class="admin-kicker">NFS-e V2</p><h3>Dados da nota fiscal</h3><p>Prestador, tomador e intermediário são participantes independentes. A Apollus pode ocupar qualquer um desses papéis.</p></div>
-  </div>
-
-  <div class="form-grid two-columns invoice-core-grid">
-    <label><span>Tipo do controle *</span><select id="finance-invoice-direction"><option value="emitida">Emitida pela Apollus</option><option value="recebida">Recebida de terceiro</option><option value="intermediada">Intermediada pela Apollus</option></select></label>
-    <label><span>Papel da Apollus *</span><select id="finance-invoice-role"><option value="prestador">Prestador / emitente</option><option value="tomador">Tomador</option><option value="intermediario">Intermediário</option><option value="nenhum">Nenhum / somente controle</option></select></label>
-    <label><span>Status da nota</span><select id="finance-invoice-status"><option value="pendente">Pendente</option><option value="emitida">Emitida</option><option value="cancelada">Cancelada</option><option value="substituida">Substituída</option></select></label>
-    <label><span>Número da NFS-e</span><input id="finance-invoice-number" maxlength="80" type="text"/></label>
-    <label><span>Data de emissão</span><input id="finance-invoice-issue-date" type="date"/></label>
-    <label><span>Competência da nota</span><input id="finance-invoice-competence" type="date"/></label>
-    <label><span>Série da DPS</span><input id="finance-invoice-dps-series" maxlength="40" type="text"/></label>
-    <label><span>Número da DPS</span><input id="finance-invoice-dps-number" maxlength="80" type="text"/></label>
-  </div>
-  <p class="invoice-role-note" id="finance-invoice-role-note"></p>
+<section class="invoice-form-section invoice-compact-section" data-requires-permission="finance.invoice">
+  <label class="settings-toggle invoice-master-toggle">
+    <input id="finance-invoice-required" type="checkbox"/>
+    <span>
+      <strong>Documentação fiscal / NFS-e</strong>
+      <small>Vincule a nota ao lançamento sem transformar esta tela em um formulário fiscal inteiro.</small>
+    </span>
+  </label>
 
-  <section class="invoice-block">
-    <header class="invoice-block-header"><div><p class="admin-kicker">Pessoas</p><h4>Participantes da NFS-e</h4></div><button class="mini-action" id="finance-party-new" type="button">+ Nova pessoa / empresa</button></header>
-    <div class="invoice-parties-grid">
-      <div class="invoice-party-field"><label><span>Prestador / emitente</span><select id="finance-invoice-provider"><option value="">Não informado</option></select></label><button class="invoice-party-edit" data-party-edit-from="finance-invoice-provider" type="button">Editar cadastro</button></div>
-      <div class="invoice-party-field"><label><span>Tomador</span><select id="finance-invoice-customer"><option value="">Não informado</option></select></label><button class="invoice-party-edit" data-party-edit-from="finance-invoice-customer" type="button">Editar cadastro</button></div>
-      <div class="invoice-party-field"><label><span>Intermediário</span><select id="finance-invoice-intermediary"><option value="">Não informado</option></select></label><button class="invoice-party-edit" data-party-edit-from="finance-invoice-intermediary" type="button">Editar cadastro</button></div>
-    </div>
-    <small class="invoice-help">Os três podem existir na mesma nota. Para notas sem intermediário, deixe esse campo como “Não informado”.</small>
-
-    <div class="invoice-party-editor" hidden="" id="finance-party-editor">
-      <input id="finance-party-id" type="hidden"/>
-      <header><div><p class="admin-kicker">Cadastro fiscal</p><h4 id="finance-party-editor-title">Nova pessoa / empresa</h4></div><button aria-label="Fechar cadastro fiscal" id="finance-party-editor-close" type="button">×</button></header>
-      <div class="form-grid two-columns">
-        <label><span>Tipo</span><select id="finance-party-type"><option value="empresa">Empresa</option><option value="pessoa">Pessoa física</option><option value="exterior">Exterior</option></select></label>
-        <label><span>Razão social / nome *</span><input id="finance-party-legal-name" maxlength="180" type="text"/></label>
-        <label><span>Nome fantasia</span><input id="finance-party-trade-name" maxlength="180" type="text"/></label>
-        <label><span>CPF / CNPJ / documento</span><input id="finance-party-tax-id" maxlength="40" type="text"/></label>
-        <label><span>Inscrição municipal</span><input id="finance-party-municipal-registration" maxlength="80" type="text"/></label>
-        <label><span>País</span><input id="finance-party-country" maxlength="2" placeholder="BR" type="text"/></label>
-        <label><span>Município</span><input id="finance-party-city" maxlength="120" type="text"/></label>
-        <label><span>UF / Estado</span><input id="finance-party-state" maxlength="80" type="text"/></label>
-        <label><span>Simples Nacional</span><select id="finance-party-simple-national"><option value="">Não informado</option><option value="true">Sim</option><option value="false">Não</option></select></label>
-        <label><span>Regime tributário</span><input id="finance-party-tax-regime" maxlength="120" placeholder="Ex.: MEI, Simples Nacional..." type="text"/></label>
+  <article class="invoice-summary-card" hidden="" id="finance-invoice-summary-card">
+    <div class="invoice-summary-copy">
+      <div class="invoice-summary-heading">
+        <span class="invoice-summary-badge" id="finance-invoice-summary-direction">NFS-e</span>
+        <span class="invoice-summary-status" id="finance-invoice-summary-status">Pendente</span>
       </div>
-      <div class="invoice-party-editor-actions"><button class="admin-btn secondary" id="finance-party-editor-cancel" type="button">Cancelar</button><button class="admin-btn primary" id="finance-party-save" type="button">Salvar cadastro</button></div>
-      <p aria-live="polite" class="form-message" id="finance-party-message"></p>
-    </div>
-  </section>
-
-  <section class="invoice-block">
-    <header class="invoice-block-header"><div><p class="admin-kicker">Serviço</p><h4>Serviço e tributação</h4></div></header>
-    <div class="form-grid two-columns">
-      <label class="form-wide"><span>Descrição do serviço</span><textarea id="finance-invoice-service" rows="3"></textarea></label>
-      <label><span>Código de Tributação Nacional</span><input id="finance-invoice-tax-code" maxlength="120" type="text"/></label>
-      <label><span>Item da NBS</span><input id="finance-invoice-nbs" maxlength="120" type="text"/></label>
-      <label><span>Município de incidência</span><input id="finance-invoice-city" maxlength="120" type="text" value="Curitiba"/></label>
-      <label><span>UF de incidência</span><input id="finance-invoice-state" maxlength="80" type="text" value="PR"/></label>
+      <strong id="finance-invoice-summary-title">Dados fiscais ainda não preenchidos</strong>
+      <p id="finance-invoice-summary-parties">Prestador, tomador e intermediário podem ser definidos separadamente.</p>
+      <small id="finance-invoice-summary-meta">Os dados fiscais serão gravados junto com o lançamento.</small>
     </div>
-  </section>
-
-  <section class="invoice-block">
-    <header class="invoice-block-header"><div><p class="admin-kicker">Valores</p><h4>Valores da NFS-e</h4></div></header>
-    <div class="form-grid two-columns">
-      <label><span>Valor do serviço</span><input id="finance-invoice-amount" min="0" step="0.01" type="number"/></label>
-      <label><span>Deduções</span><input id="finance-invoice-deductions" min="0" step="0.01" type="number" value="0"/></label>
-      <label><span>ISS</span><input id="finance-invoice-iss" min="0" step="0.01" type="number" value="0"/></label>
-      <label><span>Valor líquido</span><input id="finance-invoice-net" min="0" step="0.01" type="number"/></label>
-      <label class="form-wide invoice-drive-field"><span>Link da nota no Drive</span><div><input id="finance-invoice-url" placeholder="https://drive.google.com/..." type="url"/><a hidden="" id="finance-invoice-open-drive" rel="noopener" target="_blank">Abrir no Drive ↗</a></div><small>Use o link da pasta ou do arquivo no Google Drive. O documento não será armazenado no Supabase.</small></label>
-      <label class="form-wide"><span>Observações da NFS-e</span><textarea id="finance-invoice-notes" rows="3"></textarea></label>
-    </div>
-  </section>
-</div>
+    <button class="admin-btn secondary invoice-open-button" id="finance-invoice-open" type="button">Abrir dados da NFS-e →</button>
+  </article>
 </section>
 <section class="finance-installments-section" hidden="" id="finance-installments-section"><header><div><p class="admin-kicker">Parcelas e pagamentos</p><h3>Controle de recebimentos e pagamentos</h3></div></header><div id="finance-installments-list"></div></section>
 <footer class="dialog-actions"><button class="admin-btn secondary" data-admin-close="" type="button">Cancelar</button><button class="admin-btn danger" data-requires-permission="finance.delete" hidden="" id="finance-entry-delete" type="button">Mover para lixeira</button><button class="admin-btn primary" id="finance-entry-save" type="submit">Salvar lançamento</button></footer>
 <p aria-live="polite" class="form-message" id="finance-entry-message"></p>
 </form></dialog>
+<dialog class="admin-dialog invoice-dialog" id="finance-invoice-dialog">
+  <div class="admin-dialog-card invoice-dialog-card">
+    <header class="dialog-header invoice-dialog-header">
+      <div>
+        <p class="admin-kicker">Documentação fiscal</p>
+        <h2>Dados da NFS-e</h2>
+        <p>Preencha só o que se aplica à operação. Os dados serão gravados quando o lançamento financeiro for salvo.</p>
+      </div>
+      <button aria-label="Fechar dados da NFS-e" class="dialog-close" id="finance-invoice-dialog-close" type="button">×</button>
+    </header>
+
+    <div class="invoice-v2" hidden="" id="finance-invoice-fields">
+      <nav class="invoice-tabs" aria-label="Etapas dos dados da NFS-e">
+        <button class="invoice-tab active" data-invoice-tab="identification" type="button"><span>1</span> Identificação</button>
+        <button class="invoice-tab" data-invoice-tab="parties" type="button"><span>2</span> Pessoas</button>
+        <button class="invoice-tab" data-invoice-tab="service" type="button"><span>3</span> Serviço</button>
+        <button class="invoice-tab" data-invoice-tab="values" type="button"><span>4</span> Valores e documento</button>
+      </nav>
+
+      <section class="invoice-step active" data-invoice-panel="identification">
+        <div class="invoice-step-intro">
+          <p class="admin-kicker">Identificação</p>
+          <h3>Que nota é essa?</h3>
+          <p>O tipo do controle já sugere automaticamente o papel da Apollus. Só altere o papel em situações especiais.</p>
+        </div>
+
+        <div class="invoice-direction-cards">
+          <label class="invoice-choice">
+            <input checked="" name="finance-invoice-direction-choice" type="radio" value="emitida"/>
+            <span><strong>Emitida pela Apollus</strong><small>Apollus como prestador / emitente.</small></span>
+          </label>
+          <label class="invoice-choice">
+            <input name="finance-invoice-direction-choice" type="radio" value="recebida"/>
+            <span><strong>Recebida de terceiro</strong><small>Apollus normalmente como tomador.</small></span>
+          </label>
+          <label class="invoice-choice">
+            <input name="finance-invoice-direction-choice" type="radio" value="intermediada"/>
+            <span><strong>Intermediada pela Apollus</strong><small>Apollus como intermediário da operação.</small></span>
+          </label>
+        </div>
+
+        <select aria-label="Tipo do controle fiscal" hidden="" id="finance-invoice-direction">
+          <option value="emitida">Emitida pela Apollus</option>
+          <option value="recebida">Recebida de terceiro</option>
+          <option value="intermediada">Intermediada pela Apollus</option>
+        </select>
+
+        <div class="form-grid two-columns invoice-identification-grid">
+          <label><span>Status da nota</span><select id="finance-invoice-status"><option value="pendente">Pendente</option><option value="emitida">Emitida</option><option value="cancelada">Cancelada</option><option value="substituida">Substituída</option></select></label>
+          <label><span>Número da NFS-e</span><input id="finance-invoice-number" maxlength="80" type="text"/></label>
+          <label><span>Data de emissão</span><input id="finance-invoice-issue-date" type="date"/></label>
+          <label><span>Competência da nota</span><input id="finance-invoice-competence" type="date"/></label>
+          <label><span>Série da DPS</span><input id="finance-invoice-dps-series" maxlength="40" type="text"/></label>
+          <label><span>Número da DPS</span><input id="finance-invoice-dps-number" maxlength="80" type="text"/></label>
+        </div>
+
+        <details class="invoice-advanced">
+          <summary>Configuração avançada do papel da Apollus</summary>
+          <div>
+            <label><span>Papel da Apollus</span><select id="finance-invoice-role"><option value="prestador">Prestador / emitente</option><option value="tomador">Tomador</option><option value="intermediario">Intermediário</option><option value="nenhum">Nenhum / somente controle</option></select></label>
+            <p class="invoice-role-note" id="finance-invoice-role-note"></p>
+          </div>
+        </details>
+      </section>
+
+      <section class="invoice-step" data-invoice-panel="parties" hidden="">
+        <div class="invoice-step-intro invoice-step-intro-row">
+          <div>
+            <p class="admin-kicker">Pessoas</p>
+            <h3>Quem participa da NFS-e?</h3>
+            <p>Prestador, tomador e intermediário são independentes e podem coexistir na mesma nota.</p>
+          </div>
+          <button class="mini-action" id="finance-party-new" type="button">+ Nova pessoa / empresa</button>
+        </div>
+
+        <div class="invoice-parties-grid">
+          <article class="invoice-party-card">
+            <span class="invoice-party-number">01</span>
+            <label><span>Prestador / emitente</span><select id="finance-invoice-provider"><option value="">Não informado</option></select></label>
+            <button class="invoice-party-edit" data-party-edit-from="finance-invoice-provider" type="button">Editar cadastro</button>
+          </article>
+          <article class="invoice-party-card">
+            <span class="invoice-party-number">02</span>
+            <label><span>Tomador</span><select id="finance-invoice-customer"><option value="">Não informado</option></select></label>
+            <button class="invoice-party-edit" data-party-edit-from="finance-invoice-customer" type="button">Editar cadastro</button>
+          </article>
+          <article class="invoice-party-card">
+            <span class="invoice-party-number">03</span>
+            <label><span>Intermediário</span><select id="finance-invoice-intermediary"><option value="">Não informado</option></select></label>
+            <button class="invoice-party-edit" data-party-edit-from="finance-invoice-intermediary" type="button">Editar cadastro</button>
+          </article>
+        </div>
+
+        <div class="invoice-party-editor" hidden="" id="finance-party-editor">
+          <input id="finance-party-id" type="hidden"/>
+          <header>
+            <div><p class="admin-kicker">Cadastro fiscal</p><h4 id="finance-party-editor-title">Nova pessoa / empresa</h4></div>
+            <button aria-label="Fechar cadastro fiscal" id="finance-party-editor-close" type="button">×</button>
+          </header>
+          <div class="form-grid two-columns">
+            <label><span>Tipo</span><select id="finance-party-type"><option value="empresa">Empresa</option><option value="pessoa">Pessoa física</option><option value="exterior">Exterior</option></select></label>
+            <label><span>Razão social / nome *</span><input id="finance-party-legal-name" maxlength="180" type="text"/></label>
+            <label><span>Nome fantasia</span><input id="finance-party-trade-name" maxlength="180" type="text"/></label>
+            <label><span>CPF / CNPJ / documento</span><input id="finance-party-tax-id" maxlength="40" type="text"/></label>
+            <label><span>Inscrição municipal</span><input id="finance-party-municipal-registration" maxlength="80" type="text"/></label>
+            <label><span>País</span><input id="finance-party-country" maxlength="2" placeholder="BR" type="text"/></label>
+            <label><span>Município</span><input id="finance-party-city" maxlength="120" type="text"/></label>
+            <label><span>UF / Estado</span><input id="finance-party-state" maxlength="80" type="text"/></label>
+            <label><span>Simples Nacional</span><select id="finance-party-simple-national"><option value="">Não informado</option><option value="true">Sim</option><option value="false">Não</option></select></label>
+            <label><span>Regime tributário</span><input id="finance-party-tax-regime" maxlength="120" placeholder="Ex.: MEI, Simples Nacional..." type="text"/></label>
+          </div>
+          <div class="invoice-party-editor-actions">
+            <button class="admin-btn secondary" id="finance-party-editor-cancel" type="button">Cancelar</button>
+            <button class="admin-btn primary" id="finance-party-save" type="button">Salvar cadastro</button>
+          </div>
+          <p aria-live="polite" class="form-message" id="finance-party-message"></p>
+        </div>
+      </section>
+
+      <section class="invoice-step" data-invoice-panel="service" hidden="">
+        <div class="invoice-step-intro">
+          <p class="admin-kicker">Serviço</p>
+          <h3>Serviço e tributação</h3>
+          <p>Concentre aqui a descrição fiscal e os códigos necessários à operação.</p>
+        </div>
+        <div class="form-grid two-columns">
+          <label class="form-wide"><span>Descrição do serviço</span><textarea id="finance-invoice-service" rows="4"></textarea></label>
+          <label><span>Código de Tributação Nacional</span><input id="finance-invoice-tax-code" maxlength="120" type="text"/></label>
+          <label><span>Item da NBS</span><input id="finance-invoice-nbs" maxlength="120" type="text"/></label>
+          <label><span>Município de incidência</span><input id="finance-invoice-city" maxlength="120" type="text" value="Curitiba"/></label>
+          <label><span>UF de incidência</span><input id="finance-invoice-state" maxlength="80" type="text" value="PR"/></label>
+        </div>
+      </section>
+
+      <section class="invoice-step" data-invoice-panel="values" hidden="">
+        <div class="invoice-step-intro">
+          <p class="admin-kicker">Valores e documento</p>
+          <h3>Fechamento da NFS-e</h3>
+          <p>O documento continua no Drive; aqui ficam apenas os valores e o vínculo para consulta.</p>
+        </div>
+
+        <div class="invoice-values-highlight">
+          <label><span>Valor do serviço</span><input id="finance-invoice-amount" min="0" step="0.01" type="number"/></label>
+          <label><span>Deduções</span><input id="finance-invoice-deductions" min="0" step="0.01" type="number" value="0"/></label>
+          <label><span>ISS</span><input id="finance-invoice-iss" min="0" step="0.01" type="number" value="0"/></label>
+          <label><span>Valor líquido</span><input id="finance-invoice-net" min="0" step="0.01" type="number"/></label>
+        </div>
+
+        <div class="invoice-document-card">
+          <label class="invoice-drive-field">
+            <span>Link da nota no Drive</span>
+            <div><input id="finance-invoice-url" placeholder="https://drive.google.com/..." type="url"/><a hidden="" id="finance-invoice-open-drive" rel="noopener" target="_blank">Abrir no Drive ↗</a></div>
+            <small>Use o link da pasta ou do arquivo. Nenhum PDF ou XML é armazenado no Supabase.</small>
+          </label>
+          <label><span>Observações da NFS-e</span><textarea id="finance-invoice-notes" rows="3"></textarea></label>
+        </div>
+      </section>
+
+      <footer class="invoice-dialog-actions">
+        <span>As alterações desta janela serão gravadas quando você salvar o lançamento.</span>
+        <button class="admin-btn primary" id="finance-invoice-done" type="button">Concluir dados fiscais</button>
+      </footer>
+    </div>
+  </div>
+</dialog>
 <dialog class="admin-dialog" id="finance-payment-dialog"><form class="admin-dialog-card compact" id="finance-payment-form"><header class="dialog-header"><div><p class="admin-kicker">Pagamento parcial</p><h2 id="finance-payment-title">Registrar pagamento</h2></div><button aria-label="Fechar" class="dialog-close" data-admin-close="" type="button">×</button></header><input id="finance-payment-installment-id" type="hidden"/><div class="payment-balance" id="finance-payment-balance"></div><div class="form-grid two-columns"><label><span>Valor *</span><input id="finance-payment-amount" min="0.01" required="" step="0.01" type="number"/></label><label><span>Data *</span><input id="finance-payment-date" required="" type="date"/></label><label><span>Forma de pagamento</span><input id="finance-payment-method" maxlength="80" type="text"/></label></div><label><span>Observação</span><textarea id="finance-payment-notes" rows="3"></textarea></label><footer class="dialog-actions"><button class="admin-btn secondary" data-admin-close="" type="button">Cancelar</button><button class="admin-btn primary" type="submit">Registrar</button></footer><p class="form-message" id="finance-payment-message"></p></form></dialog>
 <dialog class="admin-dialog" id="access-profile-dialog"><form class="admin-dialog-card access-profile-form" id="access-profile-form"><header class="dialog-header"><div><p class="admin-kicker">Controle exclusivo do proprietário</p><h2 id="access-profile-title">Vincular usuário existente</h2></div><button aria-label="Fechar" class="dialog-close" data-admin-close="" type="button">×</button></header><input id="access-profile-user-id" type="hidden"/><div class="form-grid two-columns"><label class="form-wide"><span>UID do usuário no Supabase *</span><input id="access-profile-auth-user-id" maxlength="36" placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" required="" type="text"/><small>Copie em Authentication → Users. O usuário precisa existir antes de ser vinculado.</small></label><label><span>Nome *</span><input id="access-profile-name" maxlength="120" required="" type="text"/></label><label><span>E-mail do login *</span><input id="access-profile-email" maxlength="220" required="" type="email"/><small>Deve ser o mesmo e-mail cadastrado no Authentication.</small></label><label><span>Perfil-base</span><select id="access-profile-role"><option value="admin">Administrador</option><option value="editor">Editor</option><option value="finance">Financeiro</option><option value="custom">Personalizado</option></select></label></div><div class="access-profile-switches"><label class="settings-toggle"><input checked="" id="access-profile-active" type="checkbox"/><span><strong>Perfil ativo</strong><small>Permite entrar no painel.</small></span></label><label class="settings-toggle"><input id="access-profile-require-mfa" type="checkbox"/><span><strong>Exigir autenticação em duas etapas</strong><small>É ativada automaticamente ao conceder acesso financeiro.</small></span></label></div><section class="permissions-editor"><header><div><p class="admin-kicker">Permissões individuais</p><h3>O que essa pessoa pode fazer?</h3></div><button class="mini-action" id="access-permissions-clear" type="button">Limpar seleção</button></header><div class="access-permissions-grid" id="access-permissions-grid"></div></section><div class="security-note"><strong>Importante:</strong> remover este perfil tira o acesso ao painel, mas não apaga a conta em Authentication. Para apagar o login definitivamente, faça isso manualmente no Supabase.</div><footer class="dialog-actions"><button class="admin-btn secondary" data-admin-close="" type="button">Cancelar</button><button class="admin-btn primary" id="access-profile-save" type="submit">Salvar perfil</button></footer><p class="form-message" id="access-profile-message"></p></form></dialog>
 <dialog class="admin-dialog history-detail-dialog" id="history-detail-dialog">
@@ -791,6 +903,6 @@
 <footer class="dialog-actions"><button class="admin-btn secondary" data-admin-close="" type="button">Cancelar</button><button class="admin-btn danger" id="confirm-action" type="button">Excluir</button></footer>
 </div>
 </dialog>
-<script src="admin.js?v=20260807-stable1-nfse-v2" type="module"></script><script src="finance.js?v=20260807-stable1-nfse-v2" type="module"></script>
+<script src="admin.js?v=20260807-stable1-nfse-v2-ui2" type="module"></script><script src="finance.js?v=20260807-stable1-nfse-v2-ui2" type="module"></script>
 </body>
 </html>
diff -ruN apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/finance.js apollus_finance_nfse_v2_ui_clean/admin/finance.js
--- apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/finance.js	2026-08-07 18:31:06.000000000 +0000
+++ apollus_finance_nfse_v2_ui_clean/admin/finance.js	2026-08-07 19:58:56.759705851 +0000
@@ -746,27 +746,31 @@
   const role = el('finance-invoice-role')?.value || 'nenhum';
   const apollus = getApollusParty();
   const note = el('finance-invoice-role-note');
-  if (!note) return;
   if (apollus) {
     ['finance-invoice-provider', 'finance-invoice-customer', 'finance-invoice-intermediary'].forEach((selectId) => {
       if (el(selectId)?.value === apollus.id) el(selectId).value = '';
     });
   }
   if (role === 'nenhum') {
-    note.textContent = 'A Apollus não será vinculada automaticamente a nenhum participante desta nota.';
+    if (note) note.textContent = 'A Apollus não será vinculada automaticamente a nenhum participante desta nota.';
+    updateInvoiceSummary();
     return;
   }
   const targetId = invoiceRoleTargetSelect(role);
   if (apollus && targetId && el(targetId)) el(targetId).value = apollus.id;
-  note.textContent = apollus
-    ? `Apollus definida como ${apollusRoleLabel(role)}. Os outros participantes continuam independentes.`
-    : 'Cadastre a entidade fiscal da Apollus para preencher este papel automaticamente.';
+  if (note) {
+    note.textContent = apollus
+      ? `Apollus definida como ${apollusRoleLabel(role)}. Os outros participantes continuam independentes.`
+      : 'Cadastre a entidade fiscal da Apollus para preencher este papel automaticamente.';
+  }
+  updateInvoiceSummary();
 }
 
 function applyInvoiceDirectionPreset() {
   const direction = el('finance-invoice-direction')?.value || 'emitida';
   const suggestedRole = { emitida: 'prestador', recebida: 'tomador', intermediada: 'intermediario' }[direction] || 'nenhum';
   if (el('finance-invoice-role')) el('finance-invoice-role').value = suggestedRole;
+  syncInvoiceDirectionChoices();
   applyApollusRolePreset();
 }
 
@@ -777,6 +781,94 @@
   link.hidden = !safe;
   if (safe) link.href = safe;
   else link.removeAttribute('href');
+  updateInvoiceSummary();
+}
+
+
+function syncInvoiceDirectionChoices() {
+  const direction = el('finance-invoice-direction')?.value || 'emitida';
+  document.querySelectorAll('input[name="finance-invoice-direction-choice"]').forEach((radio) => {
+    radio.checked = radio.value === direction;
+  });
+}
+
+function setInvoiceTab(tab = 'identification') {
+  const allowed = new Set(['identification', 'parties', 'service', 'values']);
+  const next = allowed.has(tab) ? tab : 'identification';
+  document.querySelectorAll('[data-invoice-tab]').forEach((button) => {
+    const active = button.dataset.invoiceTab === next;
+    button.classList.toggle('active', active);
+    button.setAttribute('aria-selected', active ? 'true' : 'false');
+  });
+  document.querySelectorAll('[data-invoice-panel]').forEach((panel) => {
+    const active = panel.dataset.invoicePanel === next;
+    panel.hidden = !active;
+    panel.classList.toggle('active', active);
+  });
+}
+
+function updateInvoiceSummary() {
+  const summary = el('finance-invoice-summary-card');
+  if (!summary) return;
+  const enabled = Boolean(el('finance-invoice-required')?.checked);
+  summary.hidden = !enabled;
+  if (!enabled) return;
+
+  const direction = el('finance-invoice-direction')?.value || 'emitida';
+  const status = el('finance-invoice-status')?.value || 'pendente';
+  const role = el('finance-invoice-role')?.value || 'nenhum';
+  const number = el('finance-invoice-number')?.value.trim() || '';
+  const competence = el('finance-invoice-competence')?.value || '';
+  const provider = getParty(el('finance-invoice-provider')?.value);
+  const customer = getParty(el('finance-invoice-customer')?.value);
+  const intermediary = getParty(el('finance-invoice-intermediary')?.value);
+
+  const directionBadge = el('finance-invoice-summary-direction');
+  if (directionBadge) {
+    directionBadge.textContent = invoiceDirectionLabel(direction);
+    directionBadge.className = `invoice-summary-badge ${direction}`;
+  }
+
+  const statusBadge = el('finance-invoice-summary-status');
+  if (statusBadge) {
+    statusBadge.textContent = invoiceLabel(status);
+    statusBadge.className = `invoice-summary-status ${status}`;
+  }
+
+  const title = el('finance-invoice-summary-title');
+  if (title) title.textContent = number ? `NFS-e ${number}` : 'NFS-e ainda sem número';
+
+  const partyBits = [
+    `Prestador: ${provider ? partyDisplayName(provider) : 'não informado'}`,
+    `Tomador: ${customer ? partyDisplayName(customer) : 'não informado'}`,
+  ];
+  if (intermediary) partyBits.push(`Intermediário: ${partyDisplayName(intermediary)}`);
+  const parties = el('finance-invoice-summary-parties');
+  if (parties) parties.textContent = partyBits.join(' • ');
+
+  const metaBits = [];
+  if (competence) metaBits.push(`Competência ${formatDate(competence)}`);
+  if (role !== 'nenhum') metaBits.push(`Apollus como ${apollusRoleLabel(role)}`);
+  const meta = el('finance-invoice-summary-meta');
+  if (meta) meta.textContent = metaBits.length ? metaBits.join(' • ') : 'Abra os dados fiscais para completar a nota.';
+}
+
+function openInvoiceDialog(tab = 'identification') {
+  if (!el('finance-invoice-required')?.checked) return;
+  const dialog = el('finance-invoice-dialog');
+  if (!dialog) return;
+  el('finance-invoice-fields').hidden = false;
+  syncInvoiceDirectionChoices();
+  setInvoiceTab(tab);
+  updateInvoiceSummary();
+  if (!dialog.open) dialog.showModal();
+}
+
+function closeInvoiceDialog() {
+  closePartyEditor();
+  updateInvoiceSummary();
+  const dialog = el('finance-invoice-dialog');
+  if (dialog?.open) dialog.close();
 }
 
 function closePartyEditor() {
@@ -849,6 +941,7 @@
     await refreshFiscalParties(result.data.id, returnSelectId);
     closePartyEditor();
     if (returnSelectId && el(returnSelectId)) el(returnSelectId).value = result.data.id;
+    updateInvoiceSummary();
     notify('Cadastro fiscal salvo.', 'success');
   } catch (error) {
     console.error(error);
@@ -946,9 +1039,13 @@
   el('finance-invoice-deductions').value = '0';
   el('finance-invoice-iss').value = '0';
   populatePartySelects();
+  syncInvoiceDirectionChoices();
   applyApollusRolePreset();
   setInvoiceDriveLink('');
+  setInvoiceTab('identification');
   el('finance-invoice-fields').hidden = true;
+  if (el('finance-invoice-summary-card')) el('finance-invoice-summary-card').hidden = true;
+  if (el('finance-invoice-dialog')?.open) el('finance-invoice-dialog').close();
   el('finance-installments-section').hidden = true;
   el('finance-entry-delete').hidden = true;
   el('finance-entry-installments').disabled = false;
@@ -1007,7 +1104,11 @@
       el('finance-invoice-url').value = invoice.drive_url || '';
       el('finance-invoice-notes').value = invoice.notes || '';
       setInvoiceDriveLink(invoice.drive_url || '');
+      syncInvoiceDirectionChoices();
       applyApollusRolePreset();
+      updateInvoiceSummary();
+    } else {
+      updateInvoiceSummary();
     }
     el('finance-entry-delete').hidden = !hasPermission('finance.delete');
     renderInstallments(entry);
@@ -1370,23 +1471,80 @@
     if (!el('finance-invoice-required').checked && invoiceForEntry(existingEntry)) {
       el('finance-invoice-required').checked = true;
       el('finance-invoice-fields').hidden = false;
+      updateInvoiceSummary();
       notify('NFS-e já registrada: para preservar o histórico, altere o status para Cancelada em vez de remover.', 'error');
       return;
     }
+
     const enabled = el('finance-invoice-required').checked;
     el('finance-invoice-fields').hidden = !enabled;
-    if (enabled) {
-      if (!el('finance-invoice-amount').value) el('finance-invoice-amount').value = el('finance-entry-amount').value || '';
-      if (!el('finance-invoice-net').value) el('finance-invoice-net').value = el('finance-entry-amount').value || '';
-      if (!el('finance-invoice-competence').value) el('finance-invoice-competence').value = el('finance-entry-competence').value || todayIso();
-      applyInvoiceDirectionPreset();
+    if (!enabled) {
+      if (el('finance-invoice-dialog')?.open) closeInvoiceDialog();
+      updateInvoiceSummary();
+      return;
     }
+
+    if (!el('finance-invoice-amount').value) el('finance-invoice-amount').value = el('finance-entry-amount').value || '';
+    if (!el('finance-invoice-net').value) el('finance-invoice-net').value = el('finance-entry-amount').value || '';
+    if (!el('finance-invoice-competence').value) el('finance-invoice-competence').value = el('finance-entry-competence').value || todayIso();
+    applyInvoiceDirectionPreset();
+    updateInvoiceSummary();
+    openInvoiceDialog('identification');
+  });
+
+  el('finance-invoice-open')?.addEventListener('click', () => openInvoiceDialog('identification'));
+  el('finance-invoice-dialog-close')?.addEventListener('click', closeInvoiceDialog);
+  el('finance-invoice-done')?.addEventListener('click', closeInvoiceDialog);
+  document.querySelectorAll('[data-invoice-tab]').forEach((button) => button.addEventListener('click', () => setInvoiceTab(button.dataset.invoiceTab)));
+
+  document.querySelectorAll('input[name="finance-invoice-direction-choice"]').forEach((radio) => {
+    radio.addEventListener('change', () => {
+      if (!radio.checked) return;
+      el('finance-invoice-direction').value = radio.value;
+      applyInvoiceDirectionPreset();
+      updateInvoiceSummary();
+    });
+  });
+
+  el('finance-invoice-direction')?.addEventListener('change', () => {
+    syncInvoiceDirectionChoices();
+    applyInvoiceDirectionPreset();
+    updateInvoiceSummary();
   });
-  el('finance-invoice-direction')?.addEventListener('change', applyInvoiceDirectionPreset);
   el('finance-invoice-role')?.addEventListener('change', applyApollusRolePreset);
   el('finance-invoice-url')?.addEventListener('input', (event) => setInvoiceDriveLink(event.target.value.trim()));
-  el('finance-party-new')?.addEventListener('click', () => openPartyEditor());
+
+  [
+    'finance-invoice-status', 'finance-invoice-number', 'finance-invoice-issue-date',
+    'finance-invoice-competence', 'finance-invoice-dps-series', 'finance-invoice-dps-number',
+    'finance-invoice-provider', 'finance-invoice-customer', 'finance-invoice-intermediary',
+    'finance-invoice-service', 'finance-invoice-tax-code', 'finance-invoice-nbs',
+    'finance-invoice-city', 'finance-invoice-state', 'finance-invoice-amount',
+    'finance-invoice-deductions', 'finance-invoice-iss', 'finance-invoice-net',
+    'finance-invoice-notes',
+  ].forEach((id) => {
+    el(id)?.addEventListener('input', updateInvoiceSummary);
+    el(id)?.addEventListener('change', updateInvoiceSummary);
+  });
+
+  el('finance-entry-amount')?.addEventListener('input', () => {
+    if (!el('finance-invoice-required')?.checked) return;
+    if (!el('finance-invoice-amount').value) el('finance-invoice-amount').value = el('finance-entry-amount').value || '';
+    if (!el('finance-invoice-net').value) el('finance-invoice-net').value = el('finance-entry-amount').value || '';
+    updateInvoiceSummary();
+  });
+  el('finance-entry-competence')?.addEventListener('change', () => {
+    if (!el('finance-invoice-required')?.checked) return;
+    if (!el('finance-invoice-competence').value) el('finance-invoice-competence').value = el('finance-entry-competence').value || todayIso();
+    updateInvoiceSummary();
+  });
+
+  el('finance-party-new')?.addEventListener('click', () => {
+    setInvoiceTab('parties');
+    openPartyEditor();
+  });
   document.querySelectorAll('[data-party-edit-from]').forEach((button) => button.addEventListener('click', () => {
+    setInvoiceTab('parties');
     const selectId = button.dataset.partyEditFrom;
     const partyId = el(selectId)?.value || '';
     if (!partyId) return openPartyEditor('', selectId);
diff -ruN apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/login.html apollus_finance_nfse_v2_ui_clean/admin/login.html
--- apollus_nfse_ui_work/apollus_finance_nfse_v2_frontend/admin/login.html	2026-08-07 18:29:22.000000000 +0000
+++ apollus_finance_nfse_v2_ui_clean/admin/login.html	2026-08-07 19:58:56.760139114 +0000
@@ -10,7 +10,7 @@
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
   <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;500;600;700;800&family=Permanent+Marker&display=swap" rel="stylesheet">
-  <link rel="stylesheet" href="admin.css?v=20260807-stable1-nfse-v2">
+  <link rel="stylesheet" href="admin.css?v=20260807-stable1-nfse-v2-ui2">
 </head>
 <body data-admin-page="login" class="admin-login-body">
   <main class="login-layout">
@@ -48,6 +48,6 @@
     </section>
   </main>
 
-  <script type="module" src="admin.js?v=20260807-stable1-nfse-v2"></script>
+  <script type="module" src="admin.js?v=20260807-stable1-nfse-v2-ui2"></script>
 </body>
 </html>
