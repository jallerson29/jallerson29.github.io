APOLLUS — FINANCEIRO + ACESSOS SEM EDGE FUNCTION V1.1

SE VOCÊ JÁ EXECUTOU finance-access-v1.sql:
1) Supabase → SQL Editor → execute sql/finance-access-no-edge-v1.1.sql.
2) Substitua admin/dashboard.html e admin/finance.js.
3) git add .
   git commit -m "Simplifica acessos sem Edge Function"
   git push

SE AINDA NÃO INSTALOU O FINANCEIRO:
1) Supabase → SQL Editor → execute sql/finance-access-v1.1-full.sql uma única vez.
2) Substitua admin/dashboard.html e admin/finance.js.
3) Faça o git push.

NÃO É NECESSÁRIO:
- supabase login
- Supabase CLI
- functions deploy
- ADMIN_REDIRECT_URL
- Edge Function

NOVOS USUÁRIOS:
1) Crie o login em Supabase → Authentication → Users.
2) Copie o UID.
3) Painel Apollus → Usuários e acessos → Vincular usuário.
4) Cole o UID, e-mail e defina permissões.

IMPORTANTE:
- Somente Jallerson pode alterar permissões.
- Financeiro continua protegido por RLS + MFA.
- Remover acesso no painel NÃO apaga a conta Auth.
